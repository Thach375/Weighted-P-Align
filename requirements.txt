# Core runtime dependencies for P-ALIGN scripts.
accelerate>=1.10.0
jsonlines>=4.0.0
latex2sympy2-extended>=1.10.0
math-verify>=0.8.0
sympy>=1.14.0
torch>=2.8.0
tqdm>=4.67.0
transformers>=4.57.0
vllm>=0.11.0

# Optional external tools:
# - LLaMA-Factory is installed separately for training.
# - oat_math_grader is optional; src/evaluation.py falls back when it is absent.
